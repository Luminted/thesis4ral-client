import {Ratio} from "../types/additionalTypes"

export const aspectRatio: Ratio = {
    numerator: 7,
    divisor: 3
}

export const tableHeightPercentage = 60;

export const virtualTableWidth = 10000;
export const virtualTableHeight = 10000;